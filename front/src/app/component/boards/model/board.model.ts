export interface IBoards {
    id?: number,
    title?: string,
    content?: string,
    description?: string,
    img? : any,
    src?: any,
    regDate?: string,
    modDate?: string,
    count?: number
    //array?: IBoards[],
    //json?: {}
}